"""
General settings for predicitive audio coding network
End all paths with a trailing slash if possible
"""

import os

# Path to root directory for where sound files are located and models will be saved
DATA_PATH = "/path/to/data/directory/"

# Path to where the project python files are located. Currently serves as legacy for Elliot's code and may be removed
PROJECT_PATH = "/path/to/project/directory/"


